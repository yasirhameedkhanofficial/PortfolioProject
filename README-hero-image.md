Place the attached image file (from the editor attachments) into this project at:

/public/hero-image.jpg

Filename: hero-image.jpg
Path: /public/hero-image.jpg

How to use:
- In development Vite serves `public` at the project root, so the image will be available at `/hero-image.jpg`.
- The `Hero` component in `src/components/Hero.tsx` already references this path.

If you want a different filename or path, update the `src` attribute in `src/components/Hero.tsx` accordingly.

Notes:
- I couldn't write the binary image into the repo automatically from the attachment. Please download the attached image and save it here as `hero-image.jpg`.
- Recommended image size: ~1200x1200 (square) for best results; smaller sizes will still work but may be scaled up.